
import React from "react";
import CartClient from "@/components/cart/CartClient";

export const metadata = {
  title: "Your Flying Gear | Cart",
  description:
    "Review your items before takeoff. Secure checkout is just a step away.",
};

const CartPage = () => {
  return <CartClient />;
};

export default CartPage;
